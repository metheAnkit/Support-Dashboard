Flask==3.0.3
flask-cors==4.0.1
pymongo==4.8.0
bcrypt==4.1.3
python-dotenv==1.0.1
requests==2.32.3
# Optional: add Rasa if you want to run the Rasa server from this environment.
# Rasa has specific Python and system dependencies; install only in Python 3.10.
rasa==3.6.20
rasa-sdk==3.6.2
